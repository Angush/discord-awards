{
  "name": "react-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@reach/router": "^1.3.1",
    "react": "^16.12.0",
    "react-bootstrap": "^1.0.0-beta.16",
    "react-bootstrap-typeahead": "^3.4.7",
    "react-cookie": "^4.0.3",
    "react-dom": "^16.12.0",
    "react-masonry-css": "^1.0.14",
    "react-scripts": "^3.2.0",
    "slug": "^2.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "postbuild": "cp _redirects build/_redirects",
    "analyze": "npx react-scripts@2.1.4 build \"--stats\" && webpack-bundle-analyzer build/bundle-stats.json"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "purgecss": "^1.4.2",
    "webpack-bundle-analyzer": "^3.6.0"
  }
}
